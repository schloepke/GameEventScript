# Copyright 2026 Stephan Schlöpke
# SPDX-License-Identifier: Apache-2.0

name: Swift CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: macos-15
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Record Swift toolchain
        run: swift --version

      - name: Build packages and verify shared conformance
        run: ./scripts/test-swift.sh

      - name: Verify Swift API surface
        run: python3 scripts/verify-swift-api.py

      - name: Upload full and compact conformance results
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: swift-conformance
          path: artifacts/swift/conformance-results/
          if-no-files-found: error
          retention-days: 14
